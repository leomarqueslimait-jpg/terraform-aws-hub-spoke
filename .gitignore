*.tfstate
*.tfstate.backup
.terraform/
*.tfvars
crash.log

# Terraform plan output — can contain the same sensitive state contents as
# .tfstate. Added once the bootstrap layer made local `terraform plan -out=`
# runs a normal part of the workflow (every other env applies straight
# through an S3 backend, so this wasn't a risk before).
*.tfplan
tfplan

# Personal study notes — ignored everywhere in project
**/DNS-RESOURCES.md
**/TGW_RESOURCES.md
**/FLOW-LOGS-RESOURCES.md
**/explanation.md
**/dns_module_overview.md
**/dns-resources-explained.md
**/FLOW-LOGS-RESOURCES.md
**/iam_role_explanation.md
**/readme.meoverride.tf
override.tf.json
.terraformrc
